[package]
name = "application"
version = "0.1.0"
edition = "2024"

# ... existing code ...
[dependencies]
tracing = { version = "0.1.43", optional = true }
domain-users = { path = "../domain-users" }
bcrypt = "0.17"
async-trait = "0.1"

[features]
tracing = ["dep:tracing"]
